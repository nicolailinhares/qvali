<%= form_for([@cliente, @servico]) do |f| %>
  <% if @servico.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@servico.errors.count, "error") %> prohibited this servico from being saved:</h2>

      <ul>
      <% @servico.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :forma_pagamento %><br />
    <%= f.number_field :forma_pagamento %>
  </div>
  <div class="field">
    <%= f.label :data_inicio %><br />
    <%= f.date_select :data_inicio %>
  </div>
  <div class="field">
    <%= f.label :data_fim %><br />
    <%= f.date_select :data_fim %>
  </div>
  <div class="field">
    <%= f.label :entrega_relatorio %><br />
    <%= f.date_select :entrega_relatorio %>
  </div>
  <%= f.hidden_field :orcamento_id %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
